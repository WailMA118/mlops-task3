# Runtime dependencies — everything the inference service needs to run.
# Pinned exact versions for reproducibility; bump deliberately, not implicitly.

pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.13.0
joblib==1.4.2
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9
holidays==0.49
PyYAML==6.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Experiment tracking / model registry
mlflow==2.13.0

# Data versioning
dvc==3.51.1
dvc-s3==3.2.0

# Data validation
great-expectations==0.18.15

# Monitoring / metrics endpoint
prometheus-client==0.20.0