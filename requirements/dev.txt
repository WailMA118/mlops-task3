# Development-only dependencies: testing, linting, formatting, notebooks,
# pre-commit hooks. Installed locally and in CI, NOT inside the production
# Docker image (see Dockerfile, which installs requirements/base.txt only).
# Builds on pipeline.txt (not just base.txt) so local dev can also run the
# EDA stage (`python -m src.pipeline.run_eda`), which needs matplotlib/seaborn.

-r pipeline.txt

# Testing
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0          # FastAPI TestClient dependency

# Lint / format
ruff==0.4.4
black==24.4.2
mypy==1.10.0

# Pre-commit
pre-commit==3.7.1

# Notebooks (kept out of the runtime image)
jupyter==1.0.0
